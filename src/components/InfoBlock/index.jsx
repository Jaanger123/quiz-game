import styles from './styles.module.scss';

const InfoBlock = () => {
    return (
        <div className={styles.infoBlock}>
            <h2>Текущий игрок:</h2>
        </div>
    );
};

export default InfoBlock;
